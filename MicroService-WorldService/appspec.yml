version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "world-task-def"
        LoadBalancerInfo:
          ContainerName: "world-container"
          ContainerPort: 80
